# Results

Place these files here after running the notebook:

- `training_curves.png` — train/val loss and accuracy over epochs
- `confusion_matrix.png` — per-class confusion on the held-out test set

The main README references both by relative path.
