# Models

After running `notebook.ipynb` on Kaggle, place these files here:

- `emotion_lstm.keras` — trained model weights
- `preprocess.npz` — mean / std / class labels for inference

These are needed by `inference.py`. They are produced by the final cells of the notebook.
